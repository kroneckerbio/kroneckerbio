function expression = fixExpression(expression)
% Standardize expression as a cell array of strings

if isempty(expression)
    expression = '';
end

if ischar(expression)
    expression = {expression};
end